type TodoItem = {
    id: number
    title: string,
    description: string,
    ownerName: string, 
    todoBy: string,
}

type User = {
    id: number
    email: string,
    name: string,
    password: string,
}